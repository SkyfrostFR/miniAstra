/**
 * DTOs de la couche applicative — projections en lecture.
 *
 * <p>Ces objets transportent des données agrégées depuis les services applicatifs
 * vers la couche API. Ils peuvent représenter des vues dénormalisées adaptées
 * aux besoins de l'interface utilisateur.
 */
package fr.miniastra.application.dto;
