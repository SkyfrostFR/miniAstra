/**
 * Services domaine — logique métier pure.
 *
 * <p>Ces services opèrent exclusivement sur des objets du domaine et ne dépendent
 * d'aucune infrastructure (pas de base de données, pas d'HTTP, pas de système de fichiers).
 *
 * <p>À ne pas confondre avec les services applicatifs ({@code application.service})
 * qui orchestrent plusieurs appels et gèrent les transactions.
 */
package fr.miniastra.domain.service;
