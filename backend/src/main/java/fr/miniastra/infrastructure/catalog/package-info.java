/**
 * Catalogues statiques en mémoire.
 *
 * <p>Contient des implémentations chargées au démarrage à partir de données statiques,
 * par exemple le référentiel des trains SNCF ({@code InMemoryTrainCatalog}).
 * Ces composants ne persistent rien — ils servent de source de données de référence.
 */
package fr.miniastra.infrastructure.catalog;
