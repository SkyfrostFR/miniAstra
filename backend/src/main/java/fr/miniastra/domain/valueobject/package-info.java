/**
 * Value Objects immutables du domaine.
 *
 * <p>Un Value Object n'a pas d'identité propre — deux instances avec les mêmes
 * valeurs sont considérées égales. Implémenter {@code equals()} et {@code hashCode()}
 * en conséquence (ou utiliser des Java records).
 *
 * <p>Exemples : {@code NumeroTrain}, {@code CodeGare}, {@code PlageHoraire}.
 */
package fr.miniastra.domain.valueobject;
