/**
 * DTOs de requête — payload entrant de l'API.
 *
 * <p>Ces objets représentent les données envoyées par le client.
 * Annoter avec les contraintes de validation Bean Validation ({@code @NotNull}, etc.).
 */
package fr.miniastra.api.dto.request;
