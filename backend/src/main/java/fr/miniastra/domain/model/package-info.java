/**
 * Modèles domaine — POJO représentant les concepts métier.
 *
 * <p>Interdit : annotations JPA ({@code @Entity}, {@code @Table}, {@code @Column}, etc.),
 * annotations Spring, dépendances vers les couches externes.
 *
 * <p>Autorisé : Lombok, Java records, annotations de validation JSR-380 si nécessaire.
 */
package fr.miniastra.domain.model;
